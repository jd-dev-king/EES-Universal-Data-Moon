# EES Universal Data Moon v1 Integration

Adds:
- environment-aware frontend API base
- Railway/PostgreSQL-backed public catalog
- schema/table/column/row-count/sample endpoints
- governed read-only SQL endpoint
- configurable CORS
- read-only hardening for the existing query service

Set DATABASE_URL only on the backend.
